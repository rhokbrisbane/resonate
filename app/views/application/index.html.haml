%p Application#index
- if signed_in?
  Your email is:
  = current_user.email
- else
  You are not signed in

= form_tag search_results_path, method: :get do
  = label_tag :q, "Search:" 
  = text_field_tag :q
